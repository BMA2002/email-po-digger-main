{
  "buildCommand": "cd ./email-po-digger-main && bun install && bun run build",
  "installCommand": "echo 'Install handled in buildCommand'",
  "outputDirectory": "./email-po-digger-main/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
